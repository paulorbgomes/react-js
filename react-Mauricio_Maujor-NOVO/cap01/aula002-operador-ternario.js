// Sintaxe abreviada para a condicional if-else

// Clausula if-else
/*
let preco = 30
let resultado = ""
if(preco < 40) {
     resultado = "Livro barato!"
} else {
    resultado = "Livro não é barato!"
}
console.log(resultado)
*/

// Operador Ternario
let preco = 100
let resultado = (preco < 40) ? "Livro barato!" : "Livro não é barato!" 
console.log(resultado)